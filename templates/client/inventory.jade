button(class="js-clearAll", disabled="{{inventory.length ? '' : 'disabled'}}") Clear inventory
| {{#each inventory}}
div.ingredient(data-idx="{{@index}}")
    h6.ingredient-title {{name}}
    ul.ingredient-list
        | {{#each effects}}
        li {{.}}
        | {{/each}}
| {{/each}}